Language: Python
Version: 3.8.8
Dependencies: numpy 1.22.3, sympy 1.10.1, cplex 20.1.0.0

Input steps:
1. Get path of .lp file which is to be solved. (If file is in same folder as this program, only file name is sufficient)
2. Run driver.py 
3. Type the file path (or name) without any spaces and press enter.
4. Output is received

Output format:

In the following text, any text inside < > is replaced by actual values and rest is exactly the same as program output.

Variables:
<variable_name>: <optimal variable value>
.
.
.

Objective function value:
<optimal objective function value>